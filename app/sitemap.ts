import type { MetadataRoute } from 'next';import { getAllProducts } from '@/lib/products';export default function sitemap(): MetadataRoute.Sitemap {const base=process.env.NEXT_PUBLIC_SITE_URL||'https://example.com';const pages=['','/shop','/about','/ingredients','/contact','/privacy','/terms'].map(p=>({url:`${base}${p}`,lastModified:new Date()}));const products=getAllProducts().map(p=>({url:`${base}/shop/${p.slug}`,lastModified:new Date()}));return[...pages,...products]}
